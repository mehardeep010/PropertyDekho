# KYA HAI: Ye example env file hai. Isko copy karke '.env' banao aur apni values bharo.
#   cp .env.example .env   (ya Windows pe: copy .env.example .env)
# Asli .env git me commit NAHI hoti (.gitignore me hai) — secrets safe rehte hain.

# ── Server ──
NODE_ENV=development
PORT=3000

# ── Database (MySQL) ──
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password_here
DB_NAME=property_mgmt
DB_POOL_LIMIT=10

# ── Auth (JWT) ──
# Production me ye ek lamba random secret hona chahiye (default chalega toh app crash hoga).
JWT_SECRET=propvault_dev_secret_change_me
JWT_EXPIRES_IN=8h
